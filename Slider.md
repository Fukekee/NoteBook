**Slider（UI)**
允许用户通过拖动滑块在一个预定范围内选择一个数值的交互控件
1. **设置菜单（最经典的应用）**：
    
    - **音量控制**：调节主音量、音乐音量、音效音量。
        
    - **画面设置**：调节亮度、对比度、渲染距离。
        
    - **灵敏度设置**：调节鼠标灵敏度、手柄摇杆死区。
        
2. **游戏内界面**：
    
    - **血量条/魔法条**：用滑动条直观地表现角色的状态。
        
    - **进度条**：显示任务完成度、加载进度、技能冷却。
        
    - **力量/精度控制**：比如高尔夫球、投篮游戏的力度条。
        
3. **任何需要直观调整数值的地方**：
    
    - 调整游戏内的时间流速、调整颜色/光照强度等。
参数：

**Interactable**——取消勾选，无法拖动
**Transition**——**状态过渡效果**。定义滑块在不同状态（普通、高亮、按下、禁用）下的外观变化方式，包括颜色变化(Color Tint)、精灵切换(Sprite Swap)和动画(Animation)。
**Fill Rect**——**填充区域**的引用。这是一个 `Image` 组件，用于显示滑块左侧已填充的部分
 事件 (Event) - **实现功能的关键**——**On Value Changed (Single)****当用户拖动滑块或者通过代码改变 Value 时，就会触发这个事件**。
 
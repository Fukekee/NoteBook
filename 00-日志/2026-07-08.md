当前AR任务完成后，是靠StoryARTaskBridge或ARTaskUnit来notifySequencer的，然后一旦StorySequencer收到AR事件完成的消息，就会切换场景回Main。这里有个小问题就是，切换场景和记录任务已经完成的时机其实不在一起更好，应该是完成AR任务就发给storySequencer,而完成任务后的用于返回main场景的确认面板无论交互与否，都应该不影响storySequencer接到消息。但现在还没改
 #待解决
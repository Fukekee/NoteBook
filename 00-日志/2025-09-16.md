#### UI—Slider
**whole Numbers**
强制滑块的值在移动时，只能以整数（1, 2, 3...）为单位变化，而不能停留在小数或分数上。

简单来说，它决定了滑块是 **“离散的”** 还是 **“连续的”**。

 #find new shortcutting 
 我发现按"ctrl"加” ：“会触发笔记属性设置

text(TMP)如果拖不进脚本的选项，应该是要改代码，示例：
原：public Text tileCountText;
改：public TextMeshProUGUI tileCountText;


#debug
字体导入——otf、ttf文件直接拖进unity，右键点击-create-textmeshpro-font asset

#debug 
go map 运行却只看得到一片绿色：
错因分析：
1、map box token 空白或过期
2、3D几何网格生成失败

#debug 
GO MAP 插件一直没用
实施的措施：
1、换了更新的GO MAP版本（3.7.1）
2、放置了有效的最新的mapbox 的token，
3、对location manager的参数进行调整：![[Pasted image 20250916174209.png]]
最后终于成功了！！！(使用的Vuforia版本为10.28.4；unity版本为2022.3.62f1c1)
（效果说明：只有街道和建筑模型，建筑高度统一，都很矮，名称信息上只显示街道名字，没有建筑地点相关信息，后期考虑加入UI显示）
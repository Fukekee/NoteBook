测试的时候点击as a host，结果显示photonCloudTimeOut,但是重开一次就好了
成功进入Arena场景后，出现了不显示TeamAssignmentPanel的问题
——原因是原来的HOST模式下，只有Host才能看到这个界面，现在是Dedicated mode


再一次的测试中，结果出现了gameNotFind的提示？？？？认真的吗（在我输好房间号点击确认后）
搞嘛呢介是————时序竞态：服务器返回房间号时，Fusion 房间可能尚未在 Photon Cloud 上完全可见，客户端立即加入会触发 GameNotFound。

 改动说明

1. 重试逻辑：遇到 ShutdownReason.GameNotFound 时自动重试，最多 4 次（共 5 次尝试）。

2. 指数退避：延迟 1.5s、3s、4.5s、6s，给服务器更多时间完成房间注册。

3. 用户提示：重试时显示倒计时和当前重试次数。

4. 其他错误：非 GameNotFound 立即失败，不重试。


修改了一下调整比赛时间的次序：保留“先出房间号再调时长”，在点“确认进入”时把滑块时长发给服务器，服务器记住后再按该时长开赛。

点完as a host,出现Fusion room create failed————原因未知

发现把定位由OSM改为高德后，还真的不会发生联机场景中人物位置偏移的problem，（roomOrigin设置出错的问题）

还剩下的问题：
呜呜~~~~~
1、相机还是没按本地玩家的来
2、比赛结束相关的功能
3、fusionRunner运行一次就missing
4、关于如何取消创建房间的操作（按close是结束进程，不是简单的关闭窗口）
_/—O、怎么这么多问题？？？？


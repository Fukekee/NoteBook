
联机场景下玩家位置被拉回不动的原因：本地 InputAuthority 端不应该让 `Render()` 覆盖 _gpsTarget，而应该让本地 GPS 直接写 target，仅让服务器的 `GpsOffset` 用于同步给其他玩家的 target。
有时间可以去把相关的没必要的log去除![[Pasted image 20260316144347.png]]![[Pasted image 20260316145047.png]]|纬度差|实际距离|

|   |   |
|---|---|
|0.0001|≈ 11.1 m|

|   |   |
|---|---|
|0.00001|≈ 1.11 m|
#经纬度

|   |   |
|---|---|
|**0.000004**|**≈ 0.44 m**|
经度也差不多
玩家的target驱动：如果要用AmapGPS,要记得把MAP对象下的snapTransformToLocationprovider组件关掉，同时在locationAdapter下的references中把对应的项都拖进去

出现这个初始化位置的问题![[Pasted image 20260316180033.png]]
![[Pasted image 20260316180447.png]]每次切换模式为amap后，就会出现这样的红色报错，同时，这个报错时机与出现target大偏移错误出现的时机一致，解决这个人物大漂移问题的关键就是让amap一开始就初始化加载好，不会要在出现这个error

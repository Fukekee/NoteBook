实现框架;
第三方插件支持：fusion做房间管理，本身的photon做云端服务器（创建房间的请求一定要过这里）
unity自带：Unity Netcode for GameObjects (NGO)

联机方式：
P2P
Host-Client
Server-Client

Dedicated server：局域网联机（LAN）/云端服务器

房间码问题：NGO 本身不提供"用房间名匹配"的能力，它只是一个传输层抽象。
传输房间码的方案：UnityRelay(需付费)
自定义[[中转服务器（TURN)]]


[[UDP]]

联机下遇到的一个问题，如果玩家是由服务器set position的话，那么玩家就是会瞬移，这样的情况下是不会触发场景物品collider上的trigger的，因为trigger触发要求物理模拟，要求一个enterTrigger的过程，一个在trigger中的状态是不够的


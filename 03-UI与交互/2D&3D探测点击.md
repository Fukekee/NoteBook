## 对应关系


|     | 自己查                                                    | 交给 EventSystem                                                   |
| --- | ------------------------------------------------------ | ---------------------------------------------------------------- |
| UI  | IMGUI：`OnGUI` 里当场画、当场读点击                               | UGUI：`GraphicRaycaster` → 找到 Graphic → `onClick` / `IPointer*`   |
| 3D  | `Camera.ScreenPointToRay` + `Physics.Raycast`，自己处理 hit | 相机上的 `PhysicsRaycaster` → 找到 Collider → `IPointerClickHandler` 等 |

共同点：

- 左边：调用方每帧（或每次按下）自己问“打中了谁”，然后自己调逻辑。没有统一事件总线。
- 右边：场景里有一套拾取器，结果丢进 `EventSystem`，物体实现接口就能收 `OnPointerClick`。

所以 3D 并不是“只能自己打射线”。UGUI 那条管线可以延伸到 3D：换一个 Raycaster 就行。

`指针（鼠标 / 触屏）`

`│`

`▼`

`EventSystem`

`│`

`├─ GraphicRaycaster → Canvas 上的 Image / Button`

`├─ PhysicsRaycaster → 3D Collider`

`└─ Physics2DRaycaster → 2D Collider`

`│`

`▼`

`IPointerClick / Drag / Enter ...`

你们项目里两边都有。

走 EventSystem（3D 版 UGUI）：

TaskItemCollectible.csLines 8-8

public class TaskItemCollectible : MonoBehaviour, IPointerClickHandler

`ClickableBrick`、`ARCollectible`、`ARTaskCompletable` 也是这套。物体要有 Collider，相机要有 `PhysicsRaycaster`，场景要有 `EventSystem`。脚本只写 `OnPointerClick`，自己不用发射线。

自己打射线（3D 版 IMGUI）：

染坊拖碗、点柴火，雕版的刀和凿子，都是 `Physics.Raycast(ray, out hit)`，命中了直接改位置或触发玩法。拖拽、贴地、layer 过滤，自己写更顺手。

## 为什么 3D 更常自己打射线

IMGUI 被淘汰，主要是正式 UI 需要保留模式的层级、动画、布局。  
3D 交互经常还是手写射线，因为：

1. 要的不只是“点到了”：拖到地面、沿 mesh 雕刻、忽略某些层，要 `hit.point`、`hit.normal`、layerMask。`OnPointerClick` 只告诉你点了谁。
2. 输入源不总是指针：从脚往下打、从工具尖端打、AR 平面命中，根本不是 EventSystem 的屏幕射线。
3. 要和 UI 抢输入时，自己组合更清楚：先问 UI 吃没吃掉这次点击，再决定 3D 是否响应。

茶摊刮奖、投扇坠就是这种混合：玩法用 `Physics.Raycast`，同时查 `EventSystem`，点到 Button 就不要拖 3D。

// 典型手写 3D 点击（示意）

var cam = Camera.main;

if (cam == null || Mouse.current == null) return;

if (EventSystem.current != null && EventSystem.current.IsPointerOverGameObject())

return; // UI 先吃掉

var screen = Mouse.current.position.ReadValue();

var ray = cam.ScreenPointToRay(screen);

if (Physics.Raycast(ray, out var hit, 100f, interactableMask))

hit.collider.GetComponent<IInteractable>()?.Interact(hit);

这和 IMGUI 一样：探测、过滤、响应都在你这边。

## 怎么选

|场景|更合适|
|---|---|
|点一下收集、点砖块、点 NPC|`PhysicsRaycaster` + `IPointerClickHandler`|
|拖拽、贴地、雕刻、刮擦、要 hit.point|自己 `Physics.Raycast`|
|3D 和 Canvas 按钮叠在一起|手写射线，并先问 EventSystem；或保证 UI 的 GraphicRaycaster 排序在前|
|从物体出发的探测（落地、前方障碍）|自己打射线，与点击无关|

类比可以记成：

- IMGUI / 手写射线 = 你当服务员，每帧去问“谁被点了”
- UGUI / PhysicsRaycaster = 你当店铺，挂个“可点击”接口，收银台（EventSystem）把客人领过来

UI 正式界面用后一种；3D 玩法里两种都合法，要精细几何信息时，自己打射线通常更合适。
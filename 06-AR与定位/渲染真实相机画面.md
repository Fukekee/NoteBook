先给camera加一个控制镜头自由旋转的脚本（`*GyroCamera*`)，再加一个Render Mode为camera的canvas，调整它的planeDistance,让UI出现在3D物体的前面（我说的是AR效果），然后在canvas下面建立一个rawImage,记得全屏，然后在它上面加脚本`*DeviceCameraBackground*`（让相机画面渲染在image上），然后做完这些还是不够，很可能运行后会出现画面上显示摄像机画面的是一个正方形，这个时候很可能是旋转的问题，`WebCamTexture` 会自动降级到最近似的支持分辨率，通常是 `1280×720` 或 `1920×1080`，而这些都是横向（Landscape）格式，会触发 90° 旋转逻辑。旋转 90° 后，RectTransform 的 本地 X 轴对应屏幕 Y 方向，本地 Y 轴对应屏幕 X 方向，但当前的宽高比计算完全没有考虑这个轴向互换，导致缩放值用错了方向。
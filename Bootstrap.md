
> 复制场景时要拖大量引用 → 用 Bootstrap 把这套装配自动化

再补一句，它的职责其实更宽：

|你说的|还可以加上|
|---|---|
|避免每个场景重复拖引用|游戏启动时统一「确保存在 + 连好线」|
|自动化 Inspector 配置|场景没配全时也能兜底（如 `RegionBootstrap.GetOrCreateMatcher()`）|
||不一定只在「复制场景」时用，第一个场景也可以靠它减配置|

所以 Bootstrap 更准确的一句话是：

> 把「本应在进游戏时做一次」的创建和连线，从各个场景里收拢到一个入口。

「复制场景痛苦」只是缺 Bootstrap 时最常见的症状，不是它的唯一定义。
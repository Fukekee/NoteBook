## 核心概念

纹理图集就是把多张小图合并成一张大图。

单独存储（4张图 = 4个文件）： 图集存储（4张图 = 1个文件）：

[图A] [图B] ┌─────────┐

[图C] [图D] │ A │ B │

│────┼────│

│ C │ D │

└─────────┘

---

## 为什么要用图集

### 解决 DrawCall 问题

GPU 每次绘制时，如果两个物体用的是不同纹理，就必须切换纹理状态，产生额外 DrawCall。

- 不用图集：图A、图B、图C 是3张不同纹理 → 至少 3 次 DrawCall
- 用图集：ABC 都在同一张大图上 → 共享同一材质 → 1 次 DrawCall

### 减少内存和加载开销

GPU 加载纹理有固定的开销，100 张 64×64 的小图远比 1 张 512×512 的图集占用更多内存和带宽（因为每张纹理都有头部信息、对齐填充等）。

## Unity 中的两种图集

### Sprite Atlas（2D 精灵图集）

专门为 2D UI / Sprite 设计，Unity 会自动打包并管理 UV 重映射。

用法：

Assets → Create → 2D → Sprite Atlas

把需要合并的 Sprite 拖进去，Unity 自动合并

### 手动 Texture Atlas（3D 用）

3D 场景中把多个物体的贴图手动拼在一张图上，然后调整 UV 坐标指向各自区域。场景里的树、石头、地面等静态物体常用这种方式。
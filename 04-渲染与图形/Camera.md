==Render Type==
一般分为 **Base**（主相机） 和 **Overlay**（叠加层）。在普通 3D 场景中保持 `Base`；若你用多个相机合成画面（例如 UI 叠加或小地图），才会用 Overlay。

> [!NOTE]
> **Overlay 相机不会单独把画面渲染到屏幕**。  
> 在 URP 里，Overlay 只能被一个 **Base 相机**“叠加合成”（Camera Stacking）。你把唯一的相机改成 Overlay 后，就没有 Base 相机去显示它的结果，所以 Game 视图变黑。

### Base 相机

- 完整、独立的渲染管线
- 自己负责天空盒、清屏、最终输出
- 一台 Base 相机可以有自己的 Camera Stack（一组 Overlay 相机）

### Overlay 相机

- 不会独立输出到屏幕，必须被某台 Base 相机的 Stack 引用才会被渲染
- 它的渲染结果叠加在 Base 相机的画面上
- Inspector 上很多选项会被禁用（详见下面）
### 用法 1：第一人称武器视图（最经典）

主相机渲染世界（FOV=60），武器穿插容易"穿墙穿模"。把武器放在 `Weapon` layer，再做一台 Overlay 相机：

- Culling Mask 只勾 `Weapon`
- FOV = 45（和主相机不同也行）
- Clear 只清深度

→ 武器永远在场景之上，且不被 60FOV 透视拉变形。

### 用法 2：永远不被遮挡的 3D UI

3D 物体（比如一个高亮的物品 highlight 球、可拾取的提示图标）：

- 放在 `UI3D` layer
- Overlay 相机只渲染该 layer
- 主相机 Culling Mask 排除该 layer

→ 这些 3D UI 物体永远画在场景之上，不会被墙挡住。

### 用法 3：透视相机分层（背景层、前景层）

某些卡通风格游戏：

- Base 相机渲染远景（雾大、亮度低）
- Overlay 相机渲染近景人物（清晰、独立 PP）
- 在 Stack 里组合成一张画面

==Field of View Axis(视野轴)==
控制视角沿垂直还是水平计算。一般保持 Vertical（垂直）。
==Field of View (FOV)==
类似相机镜头焦距。数值越大视野越广（更“鱼眼”）；越小越“长焦”。常用范围：60–80。你现在设的 **77°** 是较广角的第三人称或地图相机视野。
==Clipping Planes（裁剪平面）==

| Near | 近裁剪面 | 距离相机多近的物体才开始被渲染。太小（如 0.01）可能导致深度精度问题。默认 0.1 很常见。                             |
| ---- | ---- | ---------------------------------------------------------------------------- |
| Far  | 远裁剪面 | 相机最远能看到的距离（单位：米）。超过这个距离的物体将被裁掉不渲染。你现在设的是 **586.72**，即相机能看 586 米远。（值越大，GPU越累） |



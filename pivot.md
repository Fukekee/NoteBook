### **Pivot（枢轴点）**

- **含义**：以物体自身的 **Pivot（枢轴点/原点）** 作为操作工具的参考位置。
    
- **Pivot 位置**：是由物体的 **Transform 位置**（本地坐标原点）决定的，一般在建模软件里定义。  
    例如：
    
    - 一个立方体的 Pivot 默认在几何体中心。
        
    - 一把建模的剑，Pivot 可能在握柄底部，这样方便以握柄为基准来放置和旋转。
        
- **使用场景**：当你需要精确地围绕物体的真实原点旋转、缩放或移动时，用 Pivot。
    

---

### 2. **Center（几何中心）**

- **含义**：以所选物体（单个或多个）的 **几何中心点** 作为参考。
    
- **几何中心点**：是所选物体的 **包围盒中心**（Bounding Box Center），也就是模型整体的中心位置，而不是 Transform 原点。
    
- **多选时**：如果选中多个物体，Center 会取所有物体的 **整体几何中心**。
    
- **使用场景**：当你需要围绕整体居中操作（比如旋转一组物体，或整体缩放），用 Center 更直观。
## 1. 为什么需要 Font Asset，而不是直接用 `.ttf`

`.ttf / .otf` 是矢量轮廓（字形轮廓 + 度量信息），GPU 不能直接拿它画 UI。

TMP 要的是：

|东西|作用|
|---|---|
|Atlas Texture|把字形烤成一张（或多张）纹理|
|Character / Glyph 表|Unicode → 图集 UV、宽高、advance|
|Material|用 SDF shader 把图集画成可缩放的字|
|Face Info|行高、ascender、baseline 等排版参数|

`TMP_FontAsset` 就是这些数据的容器。Text 组件挂的不是原始字体文件，而是这个资产。

---

## 2. 文字是怎么「以图集形式」显示的

核心：TMP 不画矢量字，而是画带 UV 的四边形 mesh，采样字体图集。

字符串 "金陵"

↓ 查 CharacterTable（Unicode → Glyph）

↓ 每个字生成 1 个 quad（4 顶点）

↓ UV 指向 Atlas 上该字形的矩形区域

↓ Material 绑定那张 Atlas

↓ GPU 画 mesh → 屏幕上看到字

所以一个 `TextMeshProUGUI` 默认是：

- 1 个 mesh
- 1 个 material
- material 绑定 1 张 atlas

这也是为什么一旦需要第二张 atlas / 另一个材质，就必须出现 `TMP_SubMeshUI` 子物体。

---

## 3. SDF 是什么，为什么 TMP 几乎都用它

普通位图字体：放大就糊。  
SDF（Signed Distance Field）：图集里存的是「到字形边缘的距离」，shader 用阈值重建边缘，所以：

- 放大仍相对清晰
- 描边、阴影、软边都可以用同一张图集做

你们资产里的 `SDF` 后缀、`m_AtlasRenderMode: 4165`，就是这条路径。

---

## 4. Static vs Dynamic：图集何时被填满

Font Asset 有两种填充模式：

### Static（静态）

- 创建时就把指定字符烤进图集
- 运行时不能再加新字
- 包体可预测，适合字表固定的 UI

### Dynamic（动态）——你们项目在用

m_AtlasPopulationMode: 1 // Dynamic

- 启动时图集可以几乎是空的
- 遇到新字符 → 从源 `.ttf` 现场 rasterize → 写入 atlas → 更新 CharacterTable
- 中文特别常用：全字库太大，不可能全静态打进一张图

你们 `ChatMessageItem` 里的 `TryAddCharacters`，就是在显示前主动把字符预烘焙进动态图集，减少首次出现时的卡顿/缺字闪烁。

`ClearDynamicDataOnBuild`（思源宋、字魂开了）：打包时清空运行时烤进去的字，包体更干净，进游戏后再按需填充。

---

## 5. Multi Atlas 在字体上干什么

一张 atlas 有物理上限（你们是 1024×1024）。中文动态烤字时，字一多就会塞满。

开启：

m_IsMultiAtlasTexturesEnabled: 1

后流程变成：

Atlas[0] 满了

→ 新建 Atlas[1]

→ 新字写到 Atlas[1]

→ 使用 Atlas[1] 的字符必须用另一套 material/submesh 画

所以 Multi Atlas = 同一 Font Asset 内部允许多张图集扩展，不是「多个字体」，而是「一个字体资产挂多张纹理」。

代价：同一段文字可能跨多张 atlas → Hierarchy 里出现 `TMP_SubMeshUI`，draw call 增加。

---

## 6. 缺字时的 Fallback 链

查找顺序大致是：

1. 主 Font Asset 的 CharacterTable

2. 若 Dynamic：尝试从源字体 TryAddCharacters

3. 仍没有 → 查该 Font Asset 的 Fallback 列表

4. 再没有 → TMP Settings 的全局 Fallback

5. 还没有 → 显示 □ / 缺字符号

Fallback 的本质：另一个 Font Asset（另一张 atlas / 另一个 material）。  
所以一旦用到 fallback 字，主 mesh 画不了它，TMP 会自动建 `TMP_SubMeshUI`。

你们当前几个中文字体的 fallback 表是空的：

m_FallbackFontAssetTable: []

因此缺字时不会去借别的字体，只会依赖本字体动态添加；若源字体本身也没有该字形，就会缺字。

常见配置策略：

- 主字体：美术标题体（字少、风格强）
- Fallback：思源黑体/宋体（覆盖常用汉字）
- 再 fallback：emoji / 符号专用资产

---

## 7. 和 `TMP_SubMeshUI` 的对应关系

一张图你已经看到了，触发子物体的典型原因就四类：

1. Fallback → 不同 Font Asset / atlas
2. `<sprite>` → Sprite Asset 材质不同
3. `<font=...>` → 同一段字混用多个 Font Asset
4. Multi Atlas 溢出 → 同一字体的 Atlas[1]、Atlas[2]…

这些子物体只负责渲染，不管点击/布局，也不要手动删。
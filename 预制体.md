#### 替换现有预制件
可采用的方法是从 Hierarchy 窗口中拖动新的游戏对象并将其放到 Project 窗口中现有的预制件资源上方。替换的时候是按名称来匹配子物体和组件，所以注意子物体中不要有名称相同的，单个物体的多个组件类型上也不要相同。
#### 实例覆盖

直接对单个实例进行修改。这样做会在该特定实例上创建__实例覆盖__。
四种不同的__实例覆盖__类型：

- 覆盖属性的值
    
- 添加组件
    
- 删除组件
    
- 添加子游戏对象
预制件实例存在一些限制：无法更改属于预制件的游戏对象的父级，且无法删除属于预制件的游戏对象。但是，可以停用游戏对象，这种操作很适合取代游戏对象删除操作（这算作属性覆盖）
添加和删除的组件在 Inspector 中的相应图标上也有加减符号标示，而添加的游戏对象在 Hierarchy 窗口中的相应图标上有一个加号标示。![[Pasted image 20250928162055.png]]
预制件实例上覆盖的属性值始终优先于预制件资源的值。

预制件实例的__对齐__是一种特殊情况，与其他属性的处理方式不同。**对齐__值绝不会从预制件资源传递到预制件实例。这意味着对齐值始终可与预制件资源的对齐不同，而不是作为显式实例覆盖。具体而言，对齐表示预制件实例的根变换的** Position__ 和 **Rotation** 属性，对于矩形变换，还包括 **Width**、**Height**、**Margins**、**Anchors** 和 **Pivot** 属性。

这是因为要求预制件的多个实例采用相同的位置和旋转是极其罕见的情况。更常见的情况是希望预制件实例具有不同的位置和旋转，因此 Unity 不会将它们视为预制件覆盖。
————==因为这个特殊情况，所以我的AR项目中把场景变成prefab，但是场景中的人对象预制体的scale很大，最后AR运行后，模型实例化后，才会出现复制了一批很小的人的实例的情况。==
#### 通过预制件的实例编辑预制件
与普通游戏对象相比，预制件实例的根的 Inspector 有三个额外的控件：__Open**、**Select__ 和 **Overrides**。
**open**是打开预制体资源从而编辑
**Select** 按钮在 Project 窗口中选择用于生成此实例的预制件资源。
**Overrides** 下拉窗口可以应用或恢复单个预制件覆盖，或一次性应用或恢复所有预制件覆盖。
PS:在根游戏对象上才具有 **Overrides** 下拉选单按钮的预制件。
![[Pasted image 20250928165219.png]]![[Pasted image 20250928165406.png]]
![[Pasted image 20250928165509.png]]
![[Pasted image 20250928171023.png]]
#### 嵌套预制件
可以在其他预制件内包含预制件实例。这称为**嵌套**预制件。
 **在预制件模式下添加嵌套预制件
 ![[Pasted image 20250928171521.png]]
 也即是把一个预制件作为另一个预制件的一部分
 通过预制件的实例嵌套预制件
 ![[Pasted image 20250928172541.png]]
 #### 预制件变体：
 预制件变体继承另一个称为基础预制件的预制件的属性。对预制件变体进行的覆盖优先于基础预制件的值。预制件变体可以使用任何其他预制件作为其基础预制件（包括模型预制件或其他预制件变体）。
 如何创建——
 1、可在 Project 视图中的预制件上**右键单击**，然后选择 **Create > Prefab Variant**。
 2、将预制件实例从 Hierarchy 窗口中**拖动**到 Project 窗口中。执行此操作时会出现一个对话框，询问是要创建新的原始预制件还是预制件变体。
 ![[Pasted image 20250928173151.png]]
（图片中 下面的突变为预制件变体的图标）
在预制件模式下编辑预制件变体时，应了解应用这些覆盖（通过 Overrides 下拉窗口或上下文菜单）会使变体的变化应用于基础预制件资源。（通常不需要这样，所以应该保留为覆盖而不要应用）
 
 
 
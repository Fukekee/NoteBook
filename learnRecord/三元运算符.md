条件 ? 表达式1 : 表达式2
例子：`stickToGroundY ? groundY : centerAnchor.position.y;`
具体解释如下：
•	stickToGroundY：一个 bool 类型变量，表示“是否贴地”。
•	groundY：一个 float，表示地面的Y坐标（高度）。
•	centerAnchor.position.y：centerAnchor 是一个 Transform，它的 position.y 表示锚点物体在世界空间的Y坐标。
含义：
•	如果 stickToGroundY 为 true，结果就是 groundY（即强制贴地）。
•	如果 stickToGroundY 为 false，结果就是 centerAnchor.position.y（即跟随锚点的高度）。
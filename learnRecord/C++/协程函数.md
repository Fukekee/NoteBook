## **普通函数（Function）**

- 调用后会 **一口气从头执行到尾**。
    
- 不能中途暂停再继续。
    
- 当前帧必须执行完，如果里面有耗时运算，会 **卡住主线程**（卡顿）。
    

🔸 类似：

> “我从头到尾把事情全部做完，中途不会停。”

---

## **协程（Coroutine）**

- 能够 **分段执行**：执行 → 等待 → 再继续 → 再等待…
    
- 可以在几帧甚至几秒甚至无限长的时间内完成任务。
    
- 不会卡主线程，因为中途可以 `yield` 把控制权还给 Unity。
    

🔸 类似：

> “我做一段 → 休息一下（下一帧继续） → 再做一段 → …”

---

# 🧩 2. **写法不同**

## 普通函数

`void MyFunction() {     Debug.Log("Start");     Debug.Log("End"); }`

一次执行完。

---

## 协程函数

必须返回 `IEnumerator`：

`IEnumerator MyCoroutine() {     Debug.Log("Start");     yield return new WaitForSeconds(1f); // 等 1 秒     Debug.Log("End after 1 sec"); }`

调用方式也不一样：

`StartCoroutine(MyCoroutine());`